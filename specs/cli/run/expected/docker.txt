Docker container and image management

Usage:
  j run docker [command]

Available Commands:
  clean       Clean up Docker system (prune)
  reset       Remove all containers and images
  rm          Remove all containers
  rmi         Remove all images

Flags:
  -h, --help   help for docker

Use "j run docker [command] --help" for more information about a command.
