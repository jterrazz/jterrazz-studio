// Package config holds the registries `j` is built on — tools, scripts, run
// shortcuts, cleanables, upgraders, curated skills — and the machine registry
// the box keeps at ~/.jterrazz/config.json. A feature is an entry here, not a
// command of its own.
package config
